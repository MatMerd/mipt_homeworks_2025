[project]
name = "mipt-homeworks-2025"
version = "0.1.0"
description = "MIPT homeworks from base to production service"
readme = "README.md"
license = {text = "Apache License 2.0"}
requires-python = "==3.13.*"
dependencies = [
    "fastapi>=0.127.0",
    "httpx>=0.28.1",
    "pydantic-settings>=2.12.0",
    "ujson==5.11.0",
    "uvicorn>=0.40.0",
]

[dependency-groups]
lint = [
    "flakeheaven>=3.3.0",
    "mypy>=1.18.2",
    "ruff>=0.14.0",
]
