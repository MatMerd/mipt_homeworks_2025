services:
  api: &main_app
    build:
      context: .
      dockerfile: ./Dockerfile
    image: finalProject:${FINALPROJECT_VERSION:-latest}
    restart: always
    env_file:
      - path: .env
        required: false
    environment:
      FINALPROJECT_HOST: 0.0.0.0


